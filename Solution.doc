【problem】Reverse Words in a String

Given an input string, reverse the string word by word. 
For example,
Given s = "the sky is blue",
return "blue is sky the". 
【题解】
class Solution:
    # @param s, a string
    # @return a string
    def reverseWords(self, s):
        s = s.strip()					#delete spaces at both ends
        return ' '.join(list(reversed(s.split())))#revered and spaces to pick them up
							

